# --- QE Generator / Parlement RAG ---
# Dépendances de la version du 14/12/2025 (app.py).
# Cette version n'utilise PLUS `unstructured` / opencv / pytesseract
# (contrairement à la version déployée en décembre) : la liste est donc courte.
# Volontairement non figé pour laisser Streamlit Cloud résoudre pour sa version
# de Python. À figer (pip freeze) une fois l'app stable.

streamlit
requests
pytz
python-dotenv
pydantic

# Embeddings (tire torch + transformers automatiquement)
sentence-transformers

# Base vectorielle
# Figé par prudence (version validée en prod). app.py utilise query_points()
# (compatible >= 1.10) ; passer à une version plus récente demande un test complet.
qdrant-client==1.13.3

# Recherche juridique hybride : vecteur sparse BM25 de la requête pour la fusion
# native Qdrant sur la collection unifiée CodesJuridiques (search_articles).
# "Qdrant/bm25" = tokenisation + IDF ; plus léger que le stack sentence-transformers
# déjà présent. search_articles retombe sur la recherche 4-collections si absent.
fastembed

# Extraction de documents (upload dans l'app)
PyPDF2
python-docx
pdfplumber
